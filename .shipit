steps = FindVersion, ChangeVersion, CheckChangeLog, Manifest, DistTestExtra, Commit, Tag, MakeDist, MyUploadCPAN, Twitter, DistClean

git.tagpattern = v%v
git.push_to    = origin
twitter.config = ~/.twitterrc

